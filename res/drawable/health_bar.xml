<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <item android:id="@android:id/background">
    <shape>
        <gradient
                android:startColor="@color/health_bar_back"
                android:centerColor="@color/health_bar_back"
                android:centerY="1.0"
                android:endColor="@color/health_bar_back"
                android:angle="270"/>
    </shape>
   </item>

    <item android:id="@android:id/progress">
    <clip>
        <shape>
            <gradient
                android:startColor="@color/health_bar"
                android:centerColor="@color/health_bar"
                android:centerY="1.0"
                android:endColor="@color/health_bar"
                android:angle="270"/>
        </shape>
    </clip>
    </item>
</layer-list>
